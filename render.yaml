services:
  - type: web
    name: test-pwa
    env: node
    buildCommand: bun install && bun run build
    startCommand: bun run preview
    envVars:
      - key: NODE_VERSION
        value: 22.12.0
      - key: BUN_VERSION
        value: 1.0.7
    healthCheckPath: /
    autoDeploy: true
